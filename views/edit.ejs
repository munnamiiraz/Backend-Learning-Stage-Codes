<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Mini Project</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
  <div class="main w-full min-h-screen bg-zinc-900 text-white p-10">
    <div class="logout w-full mb-3 flex justify-end">
      <a href="/logout" class="bg-red-500 rounded-md px-3 py-2 text-sm">Logout</a>
    </div>
    <h3 class="mb-5">Edit your post</h3>
    <form method="post" action="/update/<%= post._id %>">
      <textarea class="p-3 mb-3 block outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md " name = "content"><%= post.content %></textarea>
      <input type="submit" class="px-3 py-2 bg-blue-500 rounded-md block hover:cursor-pointer" value="Edit">
    </form>
  </div>
</body>
</html>